export interface IUserInfo {
   nameSurname: string;
   phoneNumber: string;
   dateOfBirth: string;
   email: string;
};

export interface IUserBonuses {
   bonuses: string;
   cardNumber: string;
};